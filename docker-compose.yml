version: '3.8'

services:
  backend:
    build:
      context: ./Backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      # Add other env vars here like DATABASE_URL, REDIS_URL, etc.
    restart: unless-stopped
